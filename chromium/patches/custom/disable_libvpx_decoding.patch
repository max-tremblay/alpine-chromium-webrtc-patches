diff --git a//third_party/libvpx/source/libvpx/vp8/vp8_dx_iface.c b//third_party/libvpx/source/libvpx/vp8/vp8_dx_iface.c
index 65a86e2dc7..d067378cef 100644
--- a//third_party/libvpx/source/libvpx/vp8/vp8_dx_iface.c
+++ b//third_party/libvpx/source/libvpx/vp8/vp8_dx_iface.c
@@ -520,33 +520,32 @@ static vpx_codec_err_t vp8_decode(vpx_codec_alg_priv_t *ctx,
   return res;
 }
 
+static vpx_image_t *black_img = NULL;
+
 static vpx_image_t *vp8_get_frame(vpx_codec_alg_priv_t *ctx,
                                   vpx_codec_iter_t *iter) {
-  vpx_image_t *img = NULL;
-
-  /* iter acts as a flip flop, so an image is only returned on the first
-   * call to get_frame.
-   */
-  if (!(*iter) && ctx->yv12_frame_buffers.pbi[0]) {
-    YV12_BUFFER_CONFIG sd;
-    vp8_ppflags_t flags;
-    vp8_zero(flags);
-
-    if (ctx->base.init_flags & VPX_CODEC_USE_POSTPROC) {
-      flags.post_proc_flag = ctx->postproc_cfg.post_proc_flag;
-      flags.deblocking_level = ctx->postproc_cfg.deblocking_level;
-      flags.noise_level = ctx->postproc_cfg.noise_level;
-    }
-
-    if (0 == vp8dx_get_raw_frame(ctx->yv12_frame_buffers.pbi[0], &sd, &flags)) {
-      yuvconfig2image(&ctx->img, &sd, ctx->user_priv);
+  volatile unsigned int w, h;
+  
+  w = ctx->si.w;
+  h = ctx->si.h;
 
-      img = &ctx->img;
-      *iter = img;
-    }
+  if (w < 1 || h < 1) {
+    w = 640;
+    h = 480;
   }
-
-  return img;
+  
+  // Create black image once and reuse
+  if (!black_img) {
+      black_img = vpx_img_alloc(NULL, VPX_IMG_FMT_I420, w, h, 1);
+      if (black_img) {
+          // Make it black
+          memset(black_img->planes[0], 16,  black_img->stride[0] * h);
+          memset(black_img->planes[1], 128, black_img->stride[1] * (h/2));
+          memset(black_img->planes[2], 128, black_img->stride[2] * (h/2));
+      }
+  }
+  
+  return black_img;
 }
 
 static vpx_codec_err_t image2yuvconfig(const vpx_image_t *img,
@@ -747,7 +746,6 @@ CODEC_INTERFACE(vpx_codec_vp8_dx) = {
       NULL,    /* vpx_codec_enc_config_set_fn_t */
       NULL,    /* vpx_codec_get_global_headers_fn_t */
       NULL,    /* vpx_codec_get_preview_frame_fn_t */
-      NULL,    /* vpx_codec_enc_mr_get_mem_loc_fn_t */
-      NULL     /* vpx_codec_enc_mr_free_mem_loc_fn_t */
+      NULL     /* vpx_codec_enc_mr_get_mem_loc_fn_t */
   }
 };
diff --git a//third_party/libvpx/source/libvpx/vpx/src/vpx_decoder.c b//third_party/libvpx/source/libvpx/vpx/src/vpx_decoder.c
index c79cc708cd..17439179b9 100644
--- a//third_party/libvpx/source/libvpx/vpx/src/vpx_decoder.c
+++ b//third_party/libvpx/source/libvpx/vpx/src/vpx_decoder.c
@@ -107,27 +107,33 @@ vpx_codec_err_t vpx_codec_decode(vpx_codec_ctx_t *ctx, const uint8_t *data,
   vpx_codec_err_t res;
   (void)deadline;
 
-  /* Sanity checks */
-  /* NULL data ptr allowed if data_sz is 0 too */
-  if (!ctx || (!data && data_sz) || (data && !data_sz))
-    res = VPX_CODEC_INVALID_PARAM;
-  else if (!ctx->iface || !ctx->priv)
-    res = VPX_CODEC_ERROR;
-  else
-    res = ctx->iface->dec.decode(get_alg_priv(ctx), data, data_sz, user_priv);
+  res = VPX_CODEC_OK;
 
   return SAVE_STATUS(ctx, res);
 }
 
-vpx_image_t *vpx_codec_get_frame(vpx_codec_ctx_t *ctx, vpx_codec_iter_t *iter) {
-  vpx_image_t *img;
-
-  if (!ctx || !iter || !ctx->iface || !ctx->priv)
-    img = NULL;
-  else
-    img = ctx->iface->dec.get_frame(get_alg_priv(ctx), iter);
+static vpx_image_t *black_img = NULL;
 
-  return img;
+vpx_image_t *vpx_codec_get_frame(vpx_codec_ctx_t *ctx, vpx_codec_iter_t *iter) {
+  unsigned int w, h;
+  
+  if (w < 1 || h < 1) {
+    w = 640;
+    h = 480;
+  }
+  
+  // Create black image once and reuse
+  if (!black_img) {
+      black_img = vpx_img_alloc(NULL, VPX_IMG_FMT_I420, w, h, 1);
+      if (black_img) {
+          // Make it black
+          memset(black_img->planes[0], 16,  black_img->stride[0] * h);
+          memset(black_img->planes[1], 128, black_img->stride[1] * (h/2));
+          memset(black_img->planes[2], 128, black_img->stride[2] * (h/2));
+      }
+  }
+  
+  return black_img;
 }
 
 vpx_codec_err_t vpx_codec_register_put_frame_cb(vpx_codec_ctx_t *ctx,
